name: core_aggregate_api
description: Core interface for defining an aggregate api
version: 0.1.0+1


environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  analyzer:
  build:
  code_builder:
  collection:
  dart_style:
  grpc:
  meta:
  path:
  pool:
  rxdart:
  synchronized:

dev_dependencies:
  build_runner:
  build_test:
  core_analysis:
  test: ^1.19.2
