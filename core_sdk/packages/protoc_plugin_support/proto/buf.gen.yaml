# buf.gen.yaml
version: v1
managed:
  enabled: true
plugins:
  - plugin: buf.build/protocolbuffers/dart:v21.1.2
    out: ../lib
    opt:
      - grpc # Enable to prevent creation of .pbserver.dart, which reexports .pb.dart (<- what we care about)
