syntax = 'proto3';

package acme.app1.service.v1;
import 'acme/app1/entity/v1/task_entity.proto';
option (acme.app1.entity.v1.task_entity_file) = true;

service TaskEntityService {
  rpc Create(CreateRequest) returns (CreateResponse);
  rpc Get(GetRequest) returns (GetResponse);
  rpc Snapshots(SnapshotQuery) returns (SnapshotResponse) {
    option (acme.app1.entity.v1.task_snapshots) = true;
  };
  option (acme.app1.entity.v1.task_service) = true;
}

message SnapshotResponse {
  // define fields here
}

message SnapshotQuery {
  // define fields here
}

message CreateRequest {
  // define fields here
}

message CreateResponse {
  // define fields here
}

message GetRequest {
  // define fields here
}

message GetResponse {
  // define fields here
}
